<%= form_for(@concert) do |f| %>
  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class =>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.datetime_select :start_date, :class => "btn btn-simple dropdown-toggle" %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.datetime_select :end_date, :class => "btn btn-simple dropdown-toggle" %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price, :class =>"form-control" %>
  </div>
  <div class="field">
    <%= f.label :tickets_available %><br>
    <%= f.number_field :tickets_available, :class =>"form-control" %>
  </div>
  <div class="field">
  <%= f.label :venue %><br>
  <%= f.collection_select :venue_id, Venue.all.collect, :id, :name, {:include_blank => "Select Venue"} %>
</div>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
